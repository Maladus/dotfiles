# Cargo
[[ -f ~/.cargo/env ]] && source ~/.cargo/env

# Activate mise for all zsh invocations (interactive + non-interactive)
# This makes shims resolve versions correctly in ssh sessions
[[ -f ~/.local/bin/mise ]] && eval "$(~/.local/bin/mise activate zsh)"

# Point mise to the global config so tools work in any directory
export MISE_CONFIG_FILE="$HOME/mise.toml"

# Ensure UTF-8 locale so Unicode symbols (arrows, checkmarks, etc.) render correctly
# in Docker exec/SSH/devpod sessions that inherit the environment
export LANG="${LANG:-en_US.UTF-8}"
export LC_ALL="${LC_ALL:-en_US.UTF-8}"

export PATH=$HOME/bin:/usr/local/bin:$HOME/.local/bin:$PATH
[[ -d ~/.cargo/bin ]] && export PATH="$HOME/.cargo/bin:$PATH"

# opencode
export PATH="$HOME/.opencode/bin:$PATH"
export PATH="$HOME/.npm-global/bin:$PATH"
